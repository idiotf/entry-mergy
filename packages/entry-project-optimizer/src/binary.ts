// TODO

import type { Project } from '@entry-mergy/entry-utils/types'

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function encodeProjectBinary(project: Project) {
  throw TypeError(`encodeProjectBinary isn't implemented`)
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function decodeProjectBinary(data: Uint8Array<ArrayBuffer>) {
  throw TypeError(`decodeProjectBinary isn't implemented`)
}
